# Workspace History Porter

Live: https://workspace-history-porter.sociobot.in — built by the Param Factory (`browser-extension`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
